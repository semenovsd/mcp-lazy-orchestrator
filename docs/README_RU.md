# Docker MCP Orchestrator v2.0

[![License: CC BY-NC 4.0](https://img.shields.io/badge/License-CC%20BY--NC%204.0-lightgrey.svg)](https://creativecommons.org/licenses/by-nc/4.0/)
[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://www.python.org/downloads/)
[![MCP](https://img.shields.io/badge/MCP-compatible-green.svg)](https://modelcontextprotocol.io/)

**Сократите расход токенов MCP на 90%+** — Умный оркестратор с автоматическим обнаружением, семантической маршрутизацией и активацией по требованию.

[🇬🇧 English Documentation](../README.md)

---

## 🎯 Проблема

При использовании **Docker MCP Toolkit** с Cursor, Claude Desktop или другими MCP клиентами, **все инструменты всех включённых серверов загружаются одновременно**. При 8 включённых серверах это легко превышает **100+ инструментов**, потребляя **15,000-20,000 токенов на запрос** только на определения инструментов.

```
❌ Текущая ситуация: Все 117 инструментов загружены
┌─────────────────────────────────────────────────────────┐
│                      CURSOR                              │
│  Контекст: ~117 инструментов = 15,000-20,000 токенов    │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│              Docker MCP Gateway                          │
│   Context7 ─ Playwright ─ GitHub ─ PostgreSQL           │
│   Fetch ─── Redis ─────── Desktop ─ Sequential          │
│        ВСЕ 117 инструментов экспортируются              │
└─────────────────────────────────────────────────────────┘
```

## ✅ Решение v2.0

**Docker MCP Orchestrator v2.0** — умный оркестратор, который:

1. **Автоматически обнаруживает** все MCP серверы из Docker MCP Toolkit
2. **Предоставляет компактный каталог** (~800-1200 токенов) вместо полных определений инструментов
3. **Умные рекомендации** с использованием keyword + semantic анализа
4. **Активация по требованию** — загружайте только нужные инструменты
5. **Автоматические зависимости** — автоматически активирует context7 для документации
6. **Профили серверов** — готовые комбинации для типичных задач
7. **Мониторинг использования** — отслеживает и оптимизирует использование серверов

```
✅ После: 8 инструментов + компактный каталог + активация по требованию
┌─────────────────────────────────────────────────────────┐
│                      CURSOR                              │
│  Контекст: ~8 инструментов оркестратора (~500 токенов)  │
│  + Компактный каталог (~1200 токенов)                   │
│  + Только активированные серверы (~2000 токенов)        │
│  Типично: 3,000-4,000 токенов (экономия 90%+)           │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│              Docker MCP Orchestrator v2.0                 │
│   • get_capabilities() - Компактный каталог             │
│   • suggest_servers(task) - Умные рекомендации           │
│   • activate_servers([]) - Активация по требованию      │
│   • activate_profile(name) - Предустановленные профили   │
│   • monitor_usage() - Статистика использования          │
│   • optimize_servers() - Автоматическая очистка          │
└─────────────────────────────────────────────────────────┘
                           │
        subprocess: docker mcp server enable/disable
                           ▼
┌─────────────────────────────────────────────────────────┐
│              Docker MCP Gateway                          │
│   [Неактивны] Большинство серверов в покое              │
│   [Активен]   Только нужные для текущей задачи          │
└─────────────────────────────────────────────────────────┘
```

## 📊 Экономия токенов

| Сценарий | До | После | Экономия |
|----------|-----|-------|----------|
| Простой запрос | ~17,000 | ~1,700 | **90%** |
| 1 активный сервер | ~17,000 | ~3,200 | **81%** |
| 3 активных сервера | ~17,000 | ~5,000 | **71%** |
| С оптимизацией | ~17,000 | ~2,500 | **85%** |

## 🚀 Быстрый старт

### Требования

- **Python 3.11+**
- **Docker Desktop** с включённым MCP Toolkit
- **Cursor 2.x** / Claude Desktop / любой MCP клиент

### Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/semenovsd/docker-mcp-orchestrator.git
cd docker-mcp-orchestrator

# Установите через pip
pip install -e .

# Или через uv
uv pip install -e .
```

### Настройка

1. **Отключите серверы в Docker MCP Toolkit** (рекомендуется):

```bash
docker mcp server disable context7 playwright github fetch \
    desktop-commander postgres redis sequential-thinking
```

2. **Настройте MCP клиент**:

**Cursor** (`~/.cursor/mcp.json`):
```json
{
  "mcpServers": {
    "orchestrator": {
      "command": "python",
      "args": ["-m", "mcp_orchestrator"],
      "type": "stdio"
    }
  }
}
```

**Claude Desktop** (`claude_desktop_config.json`):
```json
{
  "mcpServers": {
    "orchestrator": {
      "command": "python",
      "args": ["-m", "mcp_orchestrator"]
    }
  }
}
```

3. **Перезапустите MCP клиент**

## 📖 Использование

### 1. Получить компактный каталог

```
Пользователь: Какие MCP серверы доступны?
AI: [вызывает get_capabilities()]
    Возвращает: Компактный каталог с метаданными (~1200 токенов)
```

### 2. Умные рекомендации

```
Пользователь: Нужно настроить Redis кэш для FastAPI
AI: [вызывает suggest_servers("Redis кэш для FastAPI")]
    Возвращает:
      - context7 (0.95) - Документация
      - redis (0.90) - Операции
```

### 3. Активация серверов

```
AI: [вызывает activate_servers(["context7", "redis"])]
    Возвращает: Полные определения инструментов для активированных серверов
    (~2500 токенов вместо 17000!)
```

### 4. Использование профилей

```
Пользователь: Занимаюсь веб-разработкой
AI: [вызывает activate_profile("web-development")]
    Активирует: playwright, github, context7, fetch
```

### 5. Мониторинг и оптимизация

```
AI: [вызывает monitor_usage()]
    Показывает: Статистику использования и рекомендации

AI: [вызывает optimize_servers()]
    Деактивирует: Неиспользуемые серверы (>10 мин простоя)
```

## 🛠️ Доступные инструменты

### Основные инструменты (v2.0)

| Инструмент | Описание |
|------------|----------|
| `get_capabilities()` | Компактный каталог всех серверов (~1200 токенов) |
| `suggest_servers(task)` | Умные рекомендации с оценками уверенности |
| `activate_servers([])` | Активировать серверы и получить полные инструменты |
| `activate_profile(name)` | Активировать предустановленный профиль |
| `activate_for_task(desc)` | Умная активация на основе задачи |
| `deactivate_servers([])` | Деактивировать конкретные серверы |
| `get_status()` | Текущее состояние оркестратора |
| `monitor_usage()` | Статистика использования и рекомендации |
| `optimize_servers()` | Автоматическая деактивация неиспользуемых серверов |

### Устаревшие инструменты (для совместимости)

| Инструмент | Описание |
|------------|----------|
| `list_available_servers()` | Устаревший каталог (используйте get_capabilities) |
| `activate_server(name)` | Устаревшая одиночная активация |
| `deactivate_server(name)` | Устаревшая одиночная деактивация |
| `server_info(name)` | Детальная информация о сервере |
| `sync_state()` | Синхронизация с Docker MCP Toolkit |

## 📦 Поддерживаемые серверы

Оркестратор **автоматически обнаруживает** все серверы из Docker MCP Toolkit. Популярные серверы:

| Сервер | Категория | Описание |
|--------|-----------|----------|
| `context7` | Документация | Документация библиотек |
| `playwright` | Браузер | Автоматизация, скриншоты |
| `github` | VCS | Интеграция с GitHub |
| `fetch` | Сеть | HTTP запросы |
| `desktop-commander` | Система | Файлы, команды |
| `postgres` | БД | PostgreSQL запросы |
| `redis` | БД | Redis операции |
| `sequential-thinking` | Анализ | Многошаговый анализ |

### Добавление своих серверов

Серверы **автоматически обнаруживаются** из Docker MCP Toolkit. Для настройки метаданных отредактируйте `capabilities/base.yaml`:

```yaml
servers:
  my-server:
    purpose: "Что делает этот сервер"
    covers_technologies: ["tech1", "tech2"]
    when_to_use: "Когда использовать этот сервер"
    related_servers: ["context7"]  # Автоматически активировать с
```

## 🎯 Профили серверов

Предустановленные профили для типичных задач:

- **web-development**: playwright, github, context7, fetch
- **data-science**: postgres, redis, context7
- **documentation**: context7
- **database**: postgres, redis, context7
- **browser-automation**: playwright, context7
- **full-stack**: все серверы (требует подтверждения)

## ⚠️ Известные ограничения

### Hot-Reload в Cursor

Cursor 2.x требует перезапуска MCP соединения для обнаружения новых инструментов. Это ограничение Cursor. После активации сервера:
- Оркестратор сообщает доступные инструменты
- Инструменты работают через gateway даже если не видны в UI
- Перезапустите MCP соединение в настройках Cursor если нужно

### Аутентификация

Серверы с OAuth (например GitHub) требуют настройки аутентификации в Docker MCP Toolkit перед активацией.

## 🔧 Устранение проблем

### "Docker MCP CLI not found"

Убедитесь что Docker Desktop установлен с MCP Toolkit:
1. Docker Desktop → Settings → Beta features
2. Включите "Docker MCP Toolkit"

### Состояние рассинхронизировано

```
Пользователь: Синхронизируй оркестратор
AI: [вызывает sync_state()]
```

### Активация сервера не удалась

Проверьте требует ли сервер аутентификацию:
```bash
docker mcp server inspect <server-name>
```

## 🤝 Участие в разработке

Вклад приветствуется! Пожалуйста:

1. Форкните репозиторий
2. Создайте feature branch
3. Отправьте pull request

См. [CONTRIBUTING.md](../CONTRIBUTING.md) для деталей.

## 📄 Лицензия

Проект под лицензией [CC BY-NC 4.0](https://creativecommons.org/licenses/by-nc/4.0/) — бесплатно для некоммерческого использования с указанием авторства.

**Вы можете:**
- Копировать и распространять
- Изменять и создавать на основе

**При условиях:**
- Указание авторства
- Некоммерческое использование

---

**Поставьте ⭐ если проект помог сэкономить токены!**
